[pytest]
testpaths = tests
norecursedirs = datasets runs .git __pycache__
# pytest manages warnings via its own plugin, so the filter must live here (a
# module-level warnings.filterwarnings in conftest.py is reset per test).
filterwarnings =
    ignore:`torch.jit.script_method` is deprecated:DeprecationWarning
